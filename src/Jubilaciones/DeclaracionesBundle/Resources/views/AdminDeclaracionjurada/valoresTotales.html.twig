{% extends "@JubilacionesDeclaraciones/layout.html.twig" %}
{% block title %}'{{ declaracion.organismo.nombre }}', Periodo {{declaracion.periodoAnio}}/{{declaracion.periodoMes}}{% endblock %}
{% block pageid 'admin' %}

    {% block body %}
        {# CONTENIDO ORIGINAL DEL ARCHIVO Y SIN LAS ETIQUETAS <html><body> � </body></html> #}

        <div class="grid_16">
            <table>
                <thead>
                    <tr>
                        <th>Concepto</th>
                        <th>Monto ($)</th>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <td>Remunerativo</td>
                        <td>{{ valores.totalRemunerativo | number_format(2, ',', '.') }}</td>
                    </tr>
                    <tr>
                        <td>No Remunerativo</td>
                        <td>{{ valores.totalNoRemunerativo | number_format(2, ',', '.') }}</td>
                    </tr>
                    <tr>
                        <td>Aporte Personal</td>
                        <td>{{ valores.totalApPersonal | number_format(2, ',', '.') }}</td>
                    </tr>
                    <tr>
                        <td>Contribucion Patronal</td>
                        <td>{{ valores.totalApPatronal | number_format(2, ',', '.') }}</td>
                    </tr>
                    <tr>
                        <td>Inasistencia - Suspensiones</td>
                        <td>{{ valores.totalInasisSusp | number_format(2, ',', '.') }}</td>
                    </tr>
                    <tr>
                        <td>Licencia sin goce de Haberes</td>
                        <td>{{ valores.totalLicSinSueldo | number_format(2, ',', '.') }}</td>
                    </tr>
                    <tr>
                        <td>Tarea Riesgosa</td>
                        <td>{{ valores.totalTareasRiesgoza | number_format(2, ',', '.') }}</td>
                    </tr>
                </tbody>
            </table>
            <hr>
            <table>
                <thead>
                    <tr>
                        <th>Archivo</th>
                        <th>Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <td><a href="{{path('admin_jubidat',{'id' : declaracion.id })}}">Jubi.dat</a>&nbsp;&nbsp;
                        <a href="{{path('admin_jubi1ind',{'id' : declaracion.id })}}">Jubi1.ind</a></td>
                        <td>
                            <a href="{{path('admin_declaracion_aprobar',{'id' : declaracion.id })}}">Aprobar</a>&nbsp;&nbsp;
                            <a href="{{path('admin_declaracion_rechazar',{'id' : declaracion.id })}}">Rechazar</a>&nbsp;&nbsp;
                        </td>
                    </tr>
                   
                </tbody>
            </table>
            <br>&nbsp;   
            <a href="{{path('admin_declaraciones_listar')}}"> Volver </a>&nbsp;
            <br>                                
        </div>
    {% endblock %}
    {% block javascripts %}
        <script>
            function confirmaBorrar() {
                if (confirm("Desea Eliminar un Evento???")) {
                    //location.href
                }
            }
        </script>
    {% endblock %}    