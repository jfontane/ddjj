{% extends "@JubilacionesDeclaraciones/layout.html.twig" %}
{% block title %}Datos del Organismo -> Declaraciones Juradas{% endblock %}
{% block pageid 'admin' %}

    {% block body %}
        {# CONTENIDO ORIGINAL DEL ARCHIVO Y SIN LAS ETIQUETAS <html><body> � </body></html> #}
<nav aria-label="breadcrumb">
  <ol class="breadcrumb">
    <li class="breadcrumb-item"><a href="{{ path('principal_logueado') }}">Home</a></li>
    <li class="breadcrumb-item"><a href="{{path('contralor_organismo_listar')}}">Organismos</a></li>
    <li class="breadcrumb-item"><a href="{{ path('contralor_organismo_ver',{'id':organismo.id}) }}">Ver</a></li>
    <li class="breadcrumb-item active" aria-current="page">Declaraciones Juradas</li>
  </ol>
</nav>
        <div class="grid_16">
            <table>
                <thead>
                    <tr>
                        <th>
                            Periodo 
                        </th>
                        <th>
                            T.Liquidacion 
                        </th>
                        <th>
                            Estado
                        </th>
                    </tr>
                </thead>
                <tbody>
                    {% for declaracion in organismo.declaracionesjuradas %}
                        <tr>
                            <td>
                                {{ declaracion.periodoAnio }}/{{ declaracion.periodoMes }}
                            </td>
                            <td>
                                {{ declaracion.tipoLiquidacion }}
                            </td>
                            <td>
                                {{ declaracion.estado }}
                            </td>
                        </tr>
                    {% else %}
                        <tr>
                            <td colspan='2'>
                                No tiene Declaraciones Juradas
                            </td>
                        </tr>
                    {% endfor %}    
                </tbody>
            </table>
        </div>
        <br>&nbsp;

    {% endblock %}
    {% block javascripts %}
        <script>
            function confirmaBorrar() {
                if (confirm("Desea Eliminar un Evento???")) {
                    //location.href
                }
            }
        </script>
    {% endblock %}    