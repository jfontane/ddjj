{% extends "@JubilacionesDeclaraciones/layout.html.twig" %}
{% block title %}Evento{% endblock %}
    {% block pageid 'admin' %}

        {% block body %}
            {# CONTENIDO ORIGINAL DEL ARCHIVO Y SIN LAS ETIQUETAS <html><body> � </body></html> #}

            <h1>{{ declaracion.periodoAnio }} / {{ declaracion.periodoMes }}</h1>
            <p>{{ declaracion.tipoLiquidacion | raw }}</p>
            <div class="grid_2 alpha">
                <strong>Fecha</strong>  declaracio
            </div>
            <div class="grid_2">
                <strong>Fecha Ingreso</strong> fechaIngreso 
            </div>
            <div class="grid_2">
                <strong>Estado</strong> {{ declaracion.estado }}
            </div>
            <div class="grid_3">
                <strong>ppp</strong> 
            </div>
        {% endblock %}
