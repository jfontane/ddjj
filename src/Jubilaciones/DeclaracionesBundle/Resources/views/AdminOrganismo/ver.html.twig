{% extends "@JubilacionesDeclaraciones/layout.html.twig" %}
{% block title %}Datos del Organismo{% endblock %}
{% block pageid 'admin' %}

    {% block body %}
        {# CONTENIDO ORIGINAL DEL ARCHIVO Y SIN LAS ETIQUETAS <html><body> � </body></html> #}

        <div class="grid_16">
            <table>

                <!-- ITERACIÓN SOBRE LOS EVENTOS DE UN DISERTANTE -->
                <tr>
                    <td>
                        Nombre
                    </td>
                    <td>
                        {{ organismo.nombre }}
                    </td>
                </tr>
                <tr>
                    <td>
                        Domicilio
                    </td>
                    <td>
                        {{ organismo.domicilio }}
                    </td>
                </tr>
                {% if organismo.representante %}
                    <tr>
                        <td>
                            Representante Nombres
                        </td>
                        <td>
                            {{ organismo.representante.apellido }},{{ organismo.representante.nombres }}
                        </td>
                    </tr>
                    <tr>
                        <td>
                            Representante Cuil
                        </td>
                        <td>
                            {{ organismo.representante.cuil }}
                        </td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan='2'>
                            No tiene Representante Asignado
                        </td>
                    </tr>
                {% endif %}    
            </table>
            
            <a href="{{path('admin_organismo_listar')}}"> Volver </a>&nbsp;&nbsp;
            <a href="{{ path('admin_organismo_declaraciones_por_organismo',{'id':organismo.id}) }}">DDJJ</a>

        </div>
        <br>&nbsp;

    {% endblock %}
    {% block javascripts %}
        <script>
            function confirmaBorrar() {
                if (confirm("Desea Eliminar un Evento???")) {
                    //location.href
                }
            }
        </script>
    {% endblock %}    